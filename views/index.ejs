<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Blog - Share Your Stories</title>
  <link rel="stylesheet" href="/styles/main.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>

<body>
  <div class="hero-section">
    <div class="hero-content">
      <h1><i class="fas fa-feather-alt"></i> My Blog</h1>
      <p class="hero-subtitle">Share your thoughts with the world</p>
    </div>
  </div>

  <div class="container">
    <div class="info-banner">
      <div class="info-content">
        <i class="fas fa-info-circle"></i>
        <div>
          <h3>Learning Project - API Practice</h3>
          <p>This is a demo blog built to learn API calls and CRUD operations. Anyone can create, edit, or delete posts. Feel free to experiment!</p>
        </div>
      </div>
    </div>

    <div class="header-actions">
      <a id="newPostBtn" href="/new">
        <i class="fas fa-plus-circle"></i> Create New Post
      </a>
    </div>

    <div id="postsList">
      <% if (posts.length === 0) { %>
        <div class="empty-state">
          <i class="fas fa-book-open"></i>
          <h3>No posts yet</h3>
          <p>Start by creating your first blog post!</p>
        </div>
      <% } else { %>
        <% posts.forEach(post=> { %>
          <article class="post-card">
            <div class="post-header">
              <h2 class="post-title"><%=post.title%></h2>
              <div class="post-meta">
                <span class="post-date">
                  <i class="far fa-calendar-alt"></i> <%=post.date %>
                </span>
                <span class="post-author">
                  <i class="fas fa-user"></i> <%=post.author%>
                </span>
              </div>
            </div>
            <div class="post-content">
              <p><%=post.content%></p>
            </div>
            <div class="post-actions">
              <a class="btn-edit" href="/edit/<%= post.id %>">
                <i class="fas fa-edit"></i> Edit
              </a>
              <a class="btn-delete" href="/api/posts/delete/<%= post.id %>">
                <i class="fas fa-trash-alt"></i> Delete
              </a>
            </div>
          </article>
        <% }); %>
      <% } %>
    </div>
  </div>

  <footer class="footer">
    <p>&copy; 2025 My Blog. Built with <i class="fas fa-heart"></i> and Node.js</p>
  </footer>

</body>

</html>